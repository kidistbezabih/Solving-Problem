class Solution:
    def isPalindrome(self, s: str) -> bool:
        st = ""
        for i in s:
            if i.isnumeric() or i.isalpha():
                st+=i.lower()
        
        return (st == st[::-1])
